/* let nome = "Caneta"
let quantidade = 10
let preco = 6.4
var imposto = 1.5
var precoFinal = preco + imposto

    podemos criar variaveis usando 'var' ou 'let', porém existem
    diferenças entre essas nomenclaturas.

console.log(nome)
console.log(quantidade)
console.log(preco)
console.log(imposto)
console.log(precoFinal)
*/

let preco = 19.90
let desconto = 0.4

console.log(19.9 * 0.6)

let precoComDesconto = preco * (1 - desconto)
console.log(precoComDesconto)

// podemos concatenar strings usando as variaveis (nome) + " " + (desconto) == nome desconto
